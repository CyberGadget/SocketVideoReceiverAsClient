<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >
    
    <ImageView
        android:id="@+id/stream_display"
        android:contentDescription="Stream Display"
	    android:layout_width="fill_parent"
	    android:layout_height="fill_parent" />

    <Button
        android:id="@+id/sendButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/stream_display"
        android:layout_alignRight="@+id/stream_display"
        android:background="#ffffdfdf"
        android:text="Send Message" />
    
    <Button
        android:id="@+id/exitButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/stream_display"
        android:layout_alignLeft="@+id/stream_display"
        android:background="#ffffdfdf"
        android:text="Exit Connection" />

    <Button
        android:id="@+id/connButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/stream_display"
        android:layout_alignTop="@+id/stream_display"
        android:background="#ffffdfdf"
        android:text="Connect" />

</RelativeLayout>
